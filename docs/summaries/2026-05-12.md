# Session summary — 2026-05-12

First substantial Claude-driven session. Bulk of the foundational rebrand + features that the rest of the project builds on.

## Completed

### Shipped to production
- **kresume → kairesume rebrand** (PR #16, merged). Site-wide rename across UI, metadata, cookie names, event names, resume template CSS class. New domain: `https://kairesume.fit`. README and all `localhost:3000` references swapped.
- **Free-plan generation cap enforcement** (PR #16). The route used to call the LLM and then decide `downloadAllowed=false` if over cap — two bugs: paid for an LLM call we couldn't use, and counter kept growing past the limit. Fix: hard 402 gate in `/api/generate` **before** the LLM call. Also: `clampFreeCount()` pins stored counts at `SIGNED_IN_FREE_GENERATIONS`, and migration `006_clamp_free_generations.sql` cleans up legacy over-counts (the "6/3" rows people had accumulated).
- **Free support feature** (PR #16). `support_tickets` table with RLS (insert own, read own; admin reads inbox), `/api/support` endpoint, dedicated `/support` page. Later (PR #19) replaced by floating popup.
- **PDF extraction fallback** (PR #16). `pdfjs-dist` primary, `pdfreader` fallback. The Cake Resume PDF that prompted this session was producing empty output from `pdfreader` alone.
- **Job description / resume token counts in the UI** (PR #16). Live JD word + token count, plus an `InputStatsCard` in results breaking down JD/resume words & tokens against the free-tier budget. When the LLM rejects inputs as too large, error now itemises `jdTokens` + `resumeTokens` + how many tokens to trim.
- **Filename change** (PR #16). ZIP contents are now `<fullname>_resume.pdf` and `<fullname>_coverletter.pdf` (name only); ZIP filename keeps `role_company_fullname`.
- **"Cheapest AI resume builder" hero tagline** (PR #16). Plus first round of SEO foundations: full Next 14 metadata API in `app/layout.tsx`, `robots.ts`, `sitemap.ts`, `WebApplication` + `FAQPage` JSON-LD, keyword-rich SEO content block.
- **Crypto chain swap: TRC-20 → BEP-20** (PR #17). User asked for BEP-20 originally; `lib/crypto.ts` rewritten around Etherscan V2 with `chainid=56`. (Later reverted back to TRC-20 + ERC-20 in DECISION 007.)

## Important changes

- **First commits as `avp0122 <andrei.vp0122@gmail.com>`.** Earlier commits were authored as `kamax` (the local git config), which Vercel blocked because `kamax1226` isn't on the project's team. Set worktree-local `user.name`/`user.email`, `--reset-author` on the commit, force-push with `--force-with-lease`. All future commits use this identity.
- **Hard gating on the free tier is now the established pattern.** Future quota work (PR #37, etc.) just changes the constant; the enforcement plumbing was laid here.

## Open problems (at end of day)

- BEP-20 was the wrong call (user wanted TRC-20 + ERC-20, not BEP-20). Caught the next day.
- PDF extraction works in dev but the Vercel serverless reliability is unproven. Will surface as a real problem on May 14.
- Supabase confirmation email still has localhost in it — the code-level `emailRedirectTo` is set, but Supabase dashboard's Site URL setting needs a manual change (out of scope for code).

## Next recommended step

Continue iterating on the same product surface area. Add the rest of the items from the user's original ask list — multi-tier Pro pricing, blurred preview for anonymous users, signup-time metadata capture, anonymous resume_uploads.
